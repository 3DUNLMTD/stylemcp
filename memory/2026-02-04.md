# 2026-02-04

- QC scan (StyleMCP): `npm run build` + `vitest --run` + ESLint + `npm audit --omit=dev` ✅ (lint warnings only)
- Change: remove server-layer pack cache; rely on `loadPack()` TTL/invalidation cache; add `?noCache=1` to `/api/demo/validate`; include `packWarnings` when present (commit `8476d02`)
- Verify: https://stylemcp.com (200) and https://stylemcp.com/api/packs (200)
- Cleanup: discarded stray uncommitted “default pack via env var” changes; repo now clean on `8476d02`
- Re-verify: `npm run build`, `npm run lint` (warnings only), `npm test -- --run`, `npm audit --omit=dev` (0 vulns); DeepSeek review: **APPROVED** (empty diff)
