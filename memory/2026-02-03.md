# 2026-02-03 (CST)

- Deployed Week 1 security hardening to prod (commit 9d05902): helmet, global /api rate limiting, HTTPS-required middleware (prod), hardened GitHub webhook signature verification + tests.
- Verified https://stylemcp.com/health and /api/packs responding 200 after deploy.
- Implemented Week 2 input validation/limits in API (commit d697b99): MAX_TEXT_CHARS, MAX_BATCH_ITEMS, MAX_LEARN_SAMPLES enforced across key endpoints + MCP call.
- QC scan: fixed ESLint error + hardened legacy API key compare (commit 764ec80): removed unused `crypto` import + removed `legacyAuthMiddleware` from `http.ts` (billing-aware middleware handles auth); added `timingSafeEqual` compare in `src/server/middleware/auth.ts`. Ran `npm run build`, `npm test` (13 passing), `npm audit --omit=dev` (0 vulns).
- QC follow-up: improved `listAvailablePacks()` caching in `src/utils/pack-loader.ts` with TTL + directory mtime invalidation + in-flight dedupe to prevent stale pack lists and duplicate FS reads (commit 92a3fed). Lint: 10 warnings (`no-explicit-any`), 0 errors; `npm audit --omit=dev` clean.
